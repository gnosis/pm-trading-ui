import * as React from 'react'

const hairlineStyle = {
    width: '100%',
    height: '2px',
    backgroundColor: '#f0f1f1',
    marginBottom: '20px',
}

const Hairline = () => (
  <div style={hairlineStyle} />
)

export default Hairline
